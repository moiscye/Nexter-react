{
  "name": "server",
  "version": "1.0.0",
  "description": "nexter",
  "main": "index.js",
  "engines": {
    "node": "8.6.0",
    "npm": "5.0.3"
  },
  "scripts": {
    "lambda-serve": "netlify-lambda serve functions",
    "lambda-build": "netlify-lambda build functions",
    "build": "run-p build:**",
    "build:app": "react-scripts client/build",
    "build:lambda": "netlify-lambda build functions"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sendgrid/mail": "^6.5.5",
    "netlify-lambda": "^2.0.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
